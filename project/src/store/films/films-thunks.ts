// import baseAxios from '../../api/axios';
import { AppThunk } from '../store';
// import { setFilms } from './films-slice';

export const loadFilms = (): AppThunk => async (dispatch) => {
  // const path = '/films';
  // await baseAxios.get(path).then((response) => {
  //   dispatch(setFilms(response.data));
  // });
};
